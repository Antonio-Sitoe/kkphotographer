import { Cardstyle } from '../Styles/Styles';

const Cards = ({ title }) => {
  return (
    <Cardstyle>
      <p>kkphotographer</p>
      <h3>{title}</h3>
    </Cardstyle>
  );
};

export default Cards;
